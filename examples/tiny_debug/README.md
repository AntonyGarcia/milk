# Tiny Debug Example

This tiny dataset contains generated placeholder images and CSVs so you can verify dependencies, data loading, checkpointing, logging, and submission export.

Run from the repository root:

```bash
python scripts/run_tiny_debug.py
```

The debug script overrides model and training settings to keep the run small. It is not intended to produce a useful classifier.
