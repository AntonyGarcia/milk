# Core deep learning stack. For CUDA-specific PyTorch wheels, follow pytorch.org first,
# then install the remaining packages with this file if needed.
torch>=2.2
torchvision>=0.17
timm>=0.9.12
numpy>=1.24
pandas>=2.0
Pillow>=10.0
scikit-learn>=1.3
pytest>=8.0
