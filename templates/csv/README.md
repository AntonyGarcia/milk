# CSV Templates

Copy the relevant template into a dataset `metadata/` folder and replace the example rows with real records.

The training script tolerates missing optional columns and missing modalities.
